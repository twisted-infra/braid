This service ({serviceName}) is administered using braid[1].

For details about this particular service, see
{configDir}/README.

Binaries (including init scripts): {binDir}
Service Configuration: {configDir}
Logs: {logDir}
Runtime data (including pidfile): {runDir}

The following commands are availble through fabric):
{tasks}

[1] https://github.com/twisted-infra/braid
